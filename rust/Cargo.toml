[package]
name = "engine"
version = "0.1.0"
edition = "2021"

[lib]
# cdylib for the wasm artifact, rlib so `cargo test` can link the crate natively.
crate-type = ["cdylib", "rlib"]

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
codegen-units = 1
